{
  "name": "@mkizka/renovate-config",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "prettier": "^3.6.2",
    "renovate": "^41.112.0"
  },
  "lint-staged": {
    "!(package).json": "renovate-config-validator",
    "*": "prettier --write --ignore-unknown"
  }
}
