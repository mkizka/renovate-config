{
  "name": "@mkizka/renovate-config",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*": "prettier --write --ignore-unknown"
  }
}
